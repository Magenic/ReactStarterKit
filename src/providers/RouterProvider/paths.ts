interface Paths {
  contracts: string;
  dashboard: string;
  users: string;
  createUser: string;
  reporting: string;
}

export const paths: Paths = {
  dashboard: '/',
  users: '/users',
  createUser: '/users/create',
  contracts: '/contracts',
  reporting: '/reporting',
};

export default paths;
