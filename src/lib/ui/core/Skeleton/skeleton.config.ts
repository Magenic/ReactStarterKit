interface SkeletonConfig {
  color: {
    base: string;
    highlight: string;
  };
}

const config: SkeletonConfig = {
  color: {
    base: '#eee',
    highlight: '#f5f5f5',
  },
};

export default config;
